CREATE TABLE PLACEMENT
(
STUDID CHAR(10) NOT NULL,
CNAME VARCHAR(10) NOT NULL,
CPACKAGE VARCHAR(10) NOT NULL,
CLOC VARCHAR(15) NOT NULL,
CONSTRAINT FK101 FOREIGN KEY(STUDID) REFERENCES STUD1(STUDID),
CONSTRAINT CK105 CHECK(LENGTH(CPACKAGE)<=10)
);